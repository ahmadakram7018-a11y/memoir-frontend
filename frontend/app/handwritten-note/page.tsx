// Every folder inside app directory auto becomes Page URL on website
import HandwrittenNote from "@/components/landingPage/HandwrittenNote";

export default function Page() {
  return <HandwrittenNote />;
}